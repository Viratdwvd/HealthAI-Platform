apiVersion: 1

providers:
  - name: HealthAI Dashboards
    orgId: 1
    folder: HealthAI
    folderUid: healthai
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /etc/grafana/provisioning/dashboards
